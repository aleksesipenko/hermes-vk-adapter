# Tests

Planned coverage:

- text chunking;
- VK update normalization;
- group mention policy;
- user allowlist;
- Long Poll `failed` handling;
- outbound `messages.send` payload shape;
- document upload fallback flow.

Use tests only after the adapter is implemented against the actual Hermes version that will run on the VPS.
